import { icon } from "#functions";

export default {
    ptbr: {
        botNotFound: `${icon.error} | Bot não encontrado!`,
        message: `${icon.success} | Token deletado com sucesso!`
    },
    enus: {
        botNotFound: `${icon.error} | Bot not found!`,
        message: `${icon.success} | Token deleted successfully!`
    },
    eses: {
        botNotFound: `${icon.error} | ¡Bot no encontrado!`,
        message: `${icon.success} | ¡Token eliminado con éxito!`
    }
}